name: ZoltMoney CI

on:
  push:
    branches: [ main, 'feature/**' ]
  pull_request:
    branches: [ main, dev ]

jobs:
  quality-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Linting Check
        run: npm run lint

      - name: Type Checking
        run: npx tsc --noEmit

      - name: Pulse Check (Tests)
        run: npm test
